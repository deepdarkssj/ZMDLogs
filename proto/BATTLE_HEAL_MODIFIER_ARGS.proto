syntax = "proto3";

import "BATTLE_MODIFIER_HANDLE.proto";

message BATTLE_HEAL_MODIFIER_ARGS {
  uint64 healer_id = 1;
  uint64 heal_receive_id = 2;
  repeated BATTLE_MODIFIER_HANDLE healer_modifiers = 3;
  repeated BATTLE_MODIFIER_HANDLE heal_receive_modifiers = 4;
}
